{
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "backend:up": "ts-node server.ts",
    "test:e2e": "pnpm playwright test",
    "playwright:setup": "pnpm exec playwright install chromium --with-deps"
  },
  "dependencies": {
    "next": "latest",
    "react": "18.2.0",
    "react-dom": "18.2.0",
    "ts-node": "^10.9.1"
  },
  "devDependencies": {
    "@playwright/test": "^1.34.3",
    "@types/node": "^20.2.5"
  }
}
